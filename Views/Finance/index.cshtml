@model IEnumerable<kfrpj.Models.rooms.rooms_list>
@{
    ViewData["Title"] = "จัดการค่าใช้จ่าย - ระบบจัดการห้องเช่า";
}

<div class="container-fluid py-4">
    <!-- ส่วนหัวและการแจ้งเตือน -->
    <div class="row mb-4">
        <div class="col-12">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <div>
                    <h2 class="mb-1">💰 จัดการค่าใช้จ่าย</h2>
                    <p class="text-muted mb-0">เดือน <span id="currentMonthYear"></span></p>
                </div>
                <div class="btn-group">
                    <button type="button" class="btn btn-success btn-lg position-relative" id="createMissionBtn">
                        <i class="bi bi-list-check me-2"></i>Mission เก็บเงิน
                        <span class="position-absolute top-0 start-100 translate-middle badge rounded-pill bg-danger" id="missionBadge">0</span>
                    </button>
                    <button type="button" class="btn btn-info" data-bs-toggle="modal" data-bs-target="#roomChargeModal">
                        <i class="bi bi-house me-2"></i>บันทึกค่าห้อง
                    </button>
                </div>
            </div>
            
            <!-- แถบความคืบหน้าการเก็บเงิน -->
            <div class="progress-container mb-3">
                <div class="d-flex justify-content-between align-items-center mb-2">
                    <small class="text-muted">ความคืบหน้าการเก็บเงินเดือนนี้</small>
                    <small class="text-muted">อัตราการเก็บ: <span id="collectionRate">0%</span></small>
                </div>
                <div class="progress" style="height: 10px;">
                    <div class="progress-bar bg-success" role="progressbar" id="collectionProgress" style="width: 0%" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
                </div>
            </div>
        </div>
    </div>

    <!-- การ์ดแสดงสถิติ -->
    <div class="row mb-4">
        <div class="col-md-3 mb-3">
            <div class="card bg-primary text-white h-100 stat-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-title">💵 รายได้รวมเดือนนี้</h6>
                            <h3 class="mb-1" id="totalIncome">฿0</h3>
                            <small class="growth-indicator" id="incomeGrowth">+0%</small>
                        </div>
                        <i class="bi bi-cash-stack fs-1 opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3 mb-3">
            <div class="card bg-success text-white h-100 stat-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-title">✅ เก็บเงินแล้ว</h6>
                            <h3 class="mb-1" id="totalCollected">฿0</h3>
                            <small id="collectedPercentage">0% ของยอดรวม</small>
                        </div>
                        <i class="bi bi-wallet2 fs-1 opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3 mb-3">
            <div class="card bg-warning text-white h-100 stat-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-title">⏳ ค้างชำระ</h6>
                            <h3 class="mb-1" id="totalPending">฿0</h3>
                            <small id="pendingPercentage">0% ของยอดรวม</small>
                        </div>
                        <i class="bi bi-exclamation-triangle fs-1 opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
        <div class="col-md-3 mb-3">
            <div class="card bg-info text-white h-100 stat-card">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-center">
                        <div>
                            <h6 class="card-title">🏠 ห้องที่ต้องเก็บ</h6>
                            <h3 class="mb-1" id="pendingRoomsCount">0</h3>
                            <small id="averageAmount">เฉลี่ย ฿0/ห้อง</small>
                        </div>
                        <i class="bi bi-house-door fs-1 opacity-75"></i>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- เครื่องมือช่วยเหลือ -->
    <div class="card mb-4 tool-section">
        <div class="card-header bg-light">
            <h6 class="mb-0">🛠️ เครื่องมือช่วยเหลือ</h6>
        </div>
        <div class="card-body">
            <div class="row g-3">
                <div class="col-md-3">
                    <button class="btn btn-info w-100 tool-btn" id="generateMonthlyChargesBtn">
                        <i class="bi bi-calendar-plus mb-2 d-block fs-4"></i>
                        <div class="fw-bold">สร้างค่าห้องเดือนนี้</div>
                        <small class="text-muted">สร้างค่าห้องทุกห้องอัตโนมัติ</small>
                    </button>
                </div>
                <div class="col-md-3">
                    <button class="btn btn-secondary w-100 tool-btn" id="printMissionBtn">
                        <i class="bi bi-printer mb-2 d-block fs-4"></i>
                        <div class="fw-bold">พิมพ์รายการเก็บเงิน</div>
                        <small class="text-muted">พิมพ์ Mission ออกกระดาษ</small>
                    </button>
                </div>
                <div class="col-md-3">
                    <button class="btn btn-success w-100 tool-btn" id="markAllPaidBtn">
                        <i class="bi bi-check-circle-fill mb-2 d-block fs-4"></i>
                        <div class="fw-bold">เก็บเงินทุกห้อง</div>
                        <small class="text-muted">ทำเครื่องหมายเก็บครบทุกห้อง</small>
                    </button>
                </div>
                <div class="col-md-3">
                    <button class="btn btn-primary w-100 tool-btn" id="overdueReportBtn">
                        <i class="bi bi-exclamation-triangle mb-2 d-block fs-4"></i>
                        <div class="fw-bold">รายงานค้างชำระ</div>
                        <small class="text-muted">ดูรายการค้างชำระทั้งหมด</small>
                    </button>
                </div>
            </div>
        </div>
    </div>

    <!-- การตั้งค่าการแสดงผล -->
    <div class="card mb-4">
        <div class="card-body">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <h6 class="mb-0">
                        <i class="bi bi-list-check me-2"></i>Mission View - ภาพรวมทุกห้อง
                    </h6>
                </div>
                <div class="col-md-6">
                    <div class="d-flex justify-content-end align-items-center gap-3">
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="hideCompletedRooms">
                            <label class="form-check-label" for="hideCompletedRooms">
                                ซ่อนห้องที่จ่ายครบแล้ว
                            </label>
                        </div>
                        <div class="form-check form-switch">
                            <input class="form-check-input" type="checkbox" id="autoRefresh">
                            <label class="form-check-label" for="autoRefresh">
                                อัปเดตอัตโนมัติ
                            </label>
                        </div>
                        <button class="btn btn-outline-info" id="refreshDataBtn">
                            <i class="bi bi-arrow-clockwise me-1"></i>รีเฟรช
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Mission View - ตารางรวมทุกอย่าง -->
    <div class="card mission-view-card" id="missionViewCard">
        <div class="card-header bg-gradient-success text-white">
            <div class="d-flex justify-content-between align-items-center">
                <h5 class="mb-0">
                    <i class="bi bi-list-check me-2"></i>Mission เก็บเงินเดือนนี้
                </h5>
                <div class="d-flex align-items-center gap-3">
                    <span class="badge bg-light text-dark">
                        <i class="bi bi-calendar3 me-1"></i><span id="missionDate"></span>
                    </span>
                    <div class="dropdown">
                        <button class="btn btn-light btn-sm dropdown-toggle" type="button" data-bs-toggle="dropdown">
                            <i class="bi bi-funnel"></i> กรอง
                        </button>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item filter-option" href="#" data-filter="all">แสดงทั้งหมด</a></li>
                            <li><a class="dropdown-item filter-option" href="#" data-filter="unpaid">ยังไม่จ่าย</a></li>
                            <li><a class="dropdown-item filter-option" href="#" data-filter="partial">จ่ายบางส่วน</a></li>
                            <li><a class="dropdown-item filter-option" href="#" data-filter="overdue">ค้างชำระ</a></li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>
        <div class="card-body p-0">
            <div class="table-responsive">
                <table id="missionTable" class="table table-hover mb-0">
                    <thead class="table-dark sticky-top">
                        <tr>
                            <th class="text-center" style="width: 8%">
                                <input type="checkbox" class="form-check-input" id="selectAllRooms" title="เลือกทั้งหมด">
                            </th>
                            <th class="text-center" style="width: 8%">ห้อง</th>
                            <th style="width: 15%">ผู้เช่า</th>
                            <th class="text-center" style="width: 12%">ค่าห้อง</th>
                            <th class="text-center" style="width: 10%">ค่าน้ำ</th>
                            <th class="text-center" style="width: 10%">ค่าไฟ</th>
                            <th class="text-center" style="width: 12%">รวมทั้งหมด</th>
                            <th class="text-center" style="width: 10%">ความคืบหน้า</th>
                            <th class="text-center" style="width: 15%">การจัดการ</th>
                        </tr>
                    </thead>
                    <tbody id="missionTableBody">
                        <!-- ข้อมูลจะถูกโหลดด้วย JavaScript -->
                    </tbody>
                </table>
            </div>
        </div>
        <div class="card-footer">
            <div class="row align-items-center">
                <div class="col-md-6">
                    <small class="text-muted">
                        แสดง <span id="visibleRooms">0</span> จาก <span id="totalRooms">0</span> ห้อง
                    </small>
                </div>
                <div class="col-md-6 text-end">
                    <button class="btn btn-sm btn-outline-primary" id="exportMissionBtn">
                        <i class="bi bi-download me-1"></i>ส่งออกข้อมูล
                    </button>
                    <button class="btn btn-sm btn-success" id="bulkCollectBtn" disabled>
                        <i class="bi bi-check-circle me-1"></i>เก็บเงินที่เลือก
                    </button>
                </div>
            </div>
        </div>
    </div>


</div>

<!-- Modals -->
<!-- Modal สำหรับรายละเอียดห้อง -->
<div class="modal fade" id="roomDetailModal" tabindex="-1" aria-labelledby="roomDetailModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="roomDetailModalLabel">รายละเอียดค่าใช้จ่าย</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body" id="roomDetailContent">
                <!-- เนื้อหาจะถูกโหลดด้วย JavaScript -->
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ปิด</button>
                <button type="button" class="btn btn-primary" id="printRoomDetailBtn">
                    <i class="bi bi-printer me-1"></i>พิมพ์
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Modal สำหรับบันทึกค่าน้ำ -->
<div class="modal fade" id="waterBillModal" tabindex="-1" aria-labelledby="waterBillModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="waterBillModalLabel">บันทึกค่าน้ำ</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="waterBillForm">
                <div class="modal-body">
                    <input type="hidden" id="water_bill_id" name="water_bill_id">
                    <input type="hidden" id="water_room_id" name="room_id">
                    <input type="hidden" id="water_units" name="water_units">
                    <div class="mb-3">
                        <label class="form-label">ห้อง</label>
                        <input type="text" class="form-control" id="water_room_name" readonly>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="water_people_count" class="form-label">จำนวนคน <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" id="water_people_count" name="people_count" min="1" required>
                            <div class="form-text">จำนวนคนในห้อง</div>
                        </div>
                        <div class="col-md-6">
                            <label for="water_bill_amount" class="form-label">ค่าน้ำ (บาท)</label>
                            <input type="number" class="form-control" id="water_bill_amount" name="water_bill" readonly>
                            <div class="form-text">อัตรา <span id="waterRateDisplay">150</span> บาท/คน</div>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="water_bill_notes" class="form-label">หมายเหตุ</label>
                        <textarea class="form-control" id="water_bill_notes" name="notes" rows="2" placeholder="เช่น มีคนเพิ่ม, ลดราคาพิเศษ"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save me-1"></i>บันทึก
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal สำหรับบันทึกค่าไฟ -->
<div class="modal fade" id="electricBillModal" tabindex="-1" aria-labelledby="electricBillModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="electricBillModalLabel">บันทึกค่าไฟ</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="electricBillForm">
                <div class="modal-body">
                    <input type="hidden" id="electric_bill_id" name="electric_bill_id">
                    <input type="hidden" id="electric_room_id" name="room_id">
                    <input type="hidden" id="meter_date" name="meter_date">
                    <div class="mb-3">
                        <label class="form-label">ห้อง</label>
                        <input type="text" class="form-control" id="electric_room_name" readonly>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="electric_old_meter" class="form-label">เลขมิเตอร์เก่า <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" id="electric_old_meter" name="old_meter" required>
                            <div class="form-text">เลขมิเตอร์ครั้งก่อน</div>
                        </div>
                        <div class="col-md-6">
                            <label for="electric_new_meter" class="form-label">เลขมิเตอร์ใหม่ <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" id="electric_new_meter" name="new_meter" required>
                            <div class="form-text">เลขมิเตอร์ปัจจุบัน</div>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="electric_units" class="form-label">จำนวนหน่วย</label>
                            <input type="number" class="form-control" id="electric_units" name="electric_units" readonly>
                            <div class="form-text">คำนวณอัตโนมัติ</div>
                        </div>
                        <div class="col-md-6">
                            <label for="electric_bill_amount" class="form-label">ค่าไฟ (บาท)</label>
                            <input type="number" class="form-control" id="electric_bill_amount" name="electric_bill" readonly>
                            <div class="form-text">อัตรา <span id="electricRateDisplay">5</span> บาท/หน่วย</div>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="electric_bill_notes" class="form-label">หมายเหตุ</label>
                        <textarea class="form-control" id="electric_bill_notes" name="notes" rows="2" placeholder="เช่น มิเตอร์เสีย, อ่านค่าไม่ชัด"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save me-1"></i>บันทึก
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal สำหรับบันทึกค่าห้อง -->
<div class="modal fade" id="roomChargeModal" tabindex="-1" aria-labelledby="roomChargeModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="roomChargeModalLabel">บันทึกค่าห้อง</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form id="roomChargeForm">
                <div class="modal-body">
                    <input type="hidden" id="room_charge_id" name="room_charge_id">
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="room_charge_room_id" class="form-label">ห้อง <span class="text-danger">*</span></label>
                            <select class="form-select" id="room_charge_room_id" name="room_id" required>
                                <option value="">-- เลือกห้อง --</option>
                            </select>
                        </div>
                        <div class="col-md-6">
                            <label for="charge_month" class="form-label">เดือน <span class="text-danger">*</span></label>
                            <input type="month" class="form-control" id="charge_month" name="charge_month" required>
                        </div>
                    </div>
                    <div class="row mb-3">
                        <div class="col-md-6">
                            <label for="room_price" class="form-label">ค่าห้อง (บาท) <span class="text-danger">*</span></label>
                            <input type="number" class="form-control" id="room_price" name="room_price" required>
                            <div class="form-text">ค่าห้องประจำเดือน</div>
                        </div>
                        <div class="col-md-6">
                            <label for="due_date" class="form-label">วันที่ครบกำหนด <span class="text-danger">*</span></label>
                            <input type="date" class="form-control" id="due_date" name="due_date" required>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label for="room_charge_notes" class="form-label">หมายเหตุ</label>
                        <textarea class="form-control" id="room_charge_notes" name="notes" rows="2" placeholder="เช่น ลดราคาพิเศษ, เพิ่มค่าบริการ"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">ยกเลิก</button>
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-save me-1"></i>บันทึก
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal สำหรับจัดการผู้เช่า -->
<div class="modal fade" id="tenantModal" tabindex="-1" aria-labelledby="tenantModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="tenantModalLabel">จัดการผู้เช่า</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <input type="hidden" id="tenant_room_id" name="tenant_room_id">
                
                <div id="current_tenant_info" class="mb-4">
                    <div class="alert alert-info" id="no_tenant_alert">
                        <i class="bi bi-info-circle me-2"></i>ยังไม่มีผู้เช่าสำหรับห้องนี้
                    </div>
                    <div id="tenant_details" style="display: none;">
                        <div class="card border-primary">
                            <div class="card-header bg-primary text-white">
                                <h6 class="mb-0"><i class="bi bi-person-circle me-2"></i>ข้อมูลผู้เช่าปัจจุบัน</h6>
                            </div>
                            <div class="card-body">
                                <p class="mb-2"><strong>ชื่อ-นามสกุล:</strong> <span id="current_tenant_name"></span></p>
                                <p class="mb-2"><strong>อีเมล:</strong> <span id="current_tenant_email"></span></p>
                                <p class="mb-2"><strong>เบอร์โทรศัพท์:</strong> <span id="current_tenant_phone"></span></p>
                                <p class="mb-0"><strong>วันที่เริ่มสัญญา:</strong> <span id="current_tenant_start_date"></span></p>
                                <p class="mb-0"><strong>วันที่สิ้นสุดสัญญา:</strong> <span id="current_tenant_end_date"></span></p>
                            </div>
                        </div>
                        <div class="text-end mt-2">
                            <button class="btn btn-danger" id="remove_tenant_btn">
                                <i class="bi bi-person-x-fill me-2"></i>ยกเลิกผู้เช่า
                            </button>
                        </div>
                    </div>
                </div>
                
                <div id="update_tenant_section">
                    <h6><i class="bi bi-person-plus-fill me-2"></i>เลือกผู้เช่าใหม่</h6>
                    
                    <div class="mb-3">
                        <label for="tenant_select" class="form-label">เลือกผู้ใช้</label>
                        <select class="form-select" id="tenant_select">
                            <option value="">-- เลือกผู้เช่า --</option>
                        </select>
                    </div>
                    
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label for="tenant_start_date" class="form-label">วันที่เริ่มสัญญา</label>
                            <input type="date" class="form-control" id="tenant_start_date">
                        </div>
                        <div class="col-md-6 mb-3">
                            <label for="tenant_end_date" class="form-label">วันที่สิ้นสุดสัญญา</label>
                            <input type="date" class="form-control" id="tenant_end_date">
                        </div>
                    </div>
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle me-1"></i>ยกเลิก
                </button>
                <button type="button" class="btn btn-primary" id="save_tenant_btn">
                    <i class="bi bi-save me-1"></i>บันทึก
                </button>
            </div>
        </div>
    </div>
</div>

<style>
/* การปรับปรุง CSS */
.stat-card {
    transition: transform 0.2s ease-in-out;
    border: none;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.stat-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
}

.tool-btn {
    height: 120px;
    text-align: center;
    border-radius: 10px;
    transition: all 0.3s ease;
}

.tool-btn:hover {
    transform: translateY(-3px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
}

.mission-view-card {
    border: none;
    box-shadow: 0 0 20px rgba(0, 0, 0, 0.1);
}

.bg-gradient-success {
    background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
}

.growth-indicator {
    font-weight: bold;
}

.progress-container {
    background: rgba(255, 255, 255, 0.1);
    padding: 15px;
    border-radius: 10px;
    backdrop-filter: blur(10px);
}

.room-row {
    transition: all 0.3s ease;
}

.room-row:hover {
    background-color: rgba(0, 123, 255, 0.05);
}

.priority-high {
    border-left: 4px solid #dc3545;
    background-color: rgba(220, 53, 69, 0.05);
}

.priority-medium {
    border-left: 4px solid #ffc107;
    background-color: rgba(255, 193, 7, 0.05);
}

.priority-normal {
    border-left: 4px solid #28a745;
    background-color: rgba(40, 167, 69, 0.05);
}

.bill-amount {
    font-weight: bold;
    font-size: 1.1em;
}

.amount-paid {
    color: #28a745;
}

.amount-unpaid {
    color: #dc3545;
}

.amount-partial {
    color: #ffc107;
}

.sticky-top {
    position: sticky;
    top: 0;
    z-index: 1020;
}

.table th {
    border-top: none;
    font-weight: 600;
}

.progress-mini {
    height: 8px;
    border-radius: 4px;
}

@@media (max-width: 768px) {
    .tool-btn {
        height: 80px;
        font-size: 0.9em;
    }
    
    .stat-card h3 {
        font-size: 1.5rem;
    }
}

/* Dark mode support */
[data-bs-theme="dark"] .tool-section {
    background-color: #2b3035;
    border-color: #373b3e;
}

[data-bs-theme="dark"] .progress-container {
    background: rgba(0, 0, 0, 0.2);
}

[data-bs-theme="dark"] .priority-high {
    background-color: rgba(220, 53, 69, 0.1);
}

[data-bs-theme="dark"] .priority-medium {
    background-color: rgba(255, 193, 7, 0.1);
}

[data-bs-theme="dark"] .priority-normal {
    background-color: rgba(40, 167, 69, 0.1);
}

/* Loading states */
.loading {
    opacity: 0.6;
    pointer-events: none;
}

.skeleton {
    background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
    background-size: 200% 100%;
    animation: loading 1.5s infinite;
}

@@keyframes loading {
    0% {
        background-position: 200% 0;
    }
    100% {
        background-position: -200% 0;
    }
}

/* Notification styles */
.notification-badge {
    animation: pulse 2s infinite;
}

@@keyframes pulse {
    0% {
        box-shadow: 0 0 0 0 rgba(220, 53, 69, 0.7);
    }
    70% {
        box-shadow: 0 0 0 10px rgba(220, 53, 69, 0);
    }
    100% {
        box-shadow: 0 0 0 0 rgba(220, 53, 69, 0);
    }
}
</style>

@section Scripts {
    <script>
        // ตัวแปร Global
        let missionData = [];
        let currentFilter = 'all';
        let selectedRooms = new Set();
        let autoRefreshInterval = null;
        let rates = { waterRate: 15, electricRate: 5 };

        $(document).ready(function () {
            initializePage();
            loadInitialData();
            setupEventListeners();
            setupAutoRefresh();
        });

        // ฟังก์ชันเริ่มต้น
        function initializePage() {
            // ตั้งค่าวันที่ปัจจุบัน
            const now = moment();
            $('#currentMonthYear').text(now.format('MMMM YYYY'));
            $('#missionDate').text(now.format('DD/MM/YYYY'));
            
            // ตั้งค่าวันที่ในฟอร์ม
            $('#meter_date, #electric_meter_date').val(now.format('YYYY-MM-DD'));
            $('#charge_month').val(now.format('YYYY-MM'));
            
            // โหลดรายการห้อง
            loadRoomOptions();
            
            // โหลดอัตราค่าใช้จ่าย
            loadRates();
        }

        function loadInitialData() {
            updateStatistics();
            loadMissionData();
        }

        function loadMissionData() {
            console.log('เริ่มโหลดข้อมูล Mission...');
            showLoading('#missionTableBody');
            
            $.ajax({
                url: '/Finance/GetMissionData',
                type: 'GET',
                timeout: 30000, // 30 วินาที
                success: function (response) {
                    console.log('ได้รับ response:', response);
                    if (response && response.success) {
                        missionData = response.rooms || [];
                        console.log('ข้อมูล Mission:', missionData.length, 'ห้อง');
                        renderMissionTable();
                        updateMissionStats();
                    } else {
                        console.error('Response ไม่สำเร็จ:', response);
                        showError(response?.message || 'ไม่สามารถโหลดข้อมูล Mission ได้');
                        $('#missionTableBody').html('<tr><td colspan="9" class="text-center text-danger py-4">ไม่สามารถโหลดข้อมูลได้</td></tr>');
                    }
                },
                error: function (xhr, status, error) {
                    console.error('AJAX Error:', { xhr, status, error });
                    console.error('Response Text:', xhr.responseText);
                    
                    let errorMessage = 'เกิดข้อผิดพลาดในการเชื่อมต่อ';
                    if (xhr.status === 404) {
                        errorMessage = 'ไม่พบหน้าที่ต้องการ (404)';
                    } else if (xhr.status === 500) {
                        errorMessage = 'เกิดข้อผิดพลาดที่เซิร์ฟเวอร์ (500)';
                    } else if (status === 'timeout') {
                        errorMessage = 'การเชื่อมต่อหมดเวลา กรุณาลองใหม่';
                    }
                    
                    showError(errorMessage);
                    $('#missionTableBody').html('<tr><td colspan="9" class="text-center text-danger py-4">' + errorMessage + '</td></tr>');
                },
                complete: function () {
                    hideLoading('#missionTableBody');
                }
            });
        }

        function renderMissionTable() {
            console.log('เริ่ม render ตาราง Mission, missionData:', missionData);
            
            if (!missionData || !Array.isArray(missionData)) {
                console.error('missionData ไม่ใช่ array หรือ undefined:', missionData);
                $('#missionTableBody').html('<tr><td colspan="9" class="text-center text-warning py-4">ไม่มีข้อมูล</td></tr>');
                updateTableStats(0, 0);
                return;
            }

            let filteredData = applyCurrentFilter(missionData);
            console.log('ข้อมูลหลัง filter:', filteredData.length, 'จาก', missionData.length);
            
            let tableBody = '';

            if (filteredData.length === 0) {
                tableBody = '<tr><td colspan="9" class="text-center text-muted py-4">ไม่มีข้อมูลที่ตรงกับเงื่อนไข</td></tr>';
            } else {
                filteredData.forEach((room, index) => {
                    try {
                        const rowClass = getPriorityClass(room);
                        const progressWidth = room.paymentProgress || 0;
                        const progressClass = getProgressClass(progressWidth);

                        tableBody += `
                            <tr class="room-row ${rowClass}" data-room-id="${room.roomId}">
                                <td class="text-center">
                                    <input type="checkbox" class="form-check-input room-checkbox" value="${room.roomId}">
                                </td>
                                <td class="text-center">
                                    <span class="fw-bold text-primary fs-5">${room.roomName || 'ไม่ระบุ'}</span>
                                </td>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <i class="bi bi-person-circle me-2 text-muted"></i>
                                        ${room.tenantName || 'ไม่ระบุ'}
                                    </div>
                                </td>
                                <td class="text-center">
                                    <span class="bill-amount ${room.roomCharge?.isPaid ? 'amount-paid' : 'amount-unpaid'}">
                                        ฿${(room.roomCharge?.amount || 0).toLocaleString()}
                                    </span>
                                    ${room.roomCharge?.isPaid ? '<i class="bi bi-check-circle text-success ms-1"></i>' : ''}
                                </td>
                                <td class="text-center">
                                    <div class="d-flex align-items-center justify-content-center gap-1">
                                        <span class="bill-amount ${room.waterBill?.isPaid ? 'amount-paid' : 'amount-unpaid'}">
                                            ฿${(room.waterBill?.amount || 0).toLocaleString()}
                                        </span>
                                        ${(room.waterBill?.amount || 0) > 0 && !room.waterBill?.isPaid ? 
                                            `<button class="btn btn-sm btn-outline-primary" onclick="editWaterBill(${room.roomId}, '${room.roomName}')" title="แก้ไขค่าน้ำ">
                                                <i class="bi bi-pencil"></i>
                                            </button>` : ''
                                        }
                                        ${(room.waterBill?.amount || 0) === 0 ? 
                                            `<button class="btn btn-sm btn-primary" onclick="addWaterBill(${room.roomId}, '${room.roomName}')" title="เพิ่มค่าน้ำ">
                                                <i class="bi bi-plus"></i>
                                            </button>` : ''
                                        }
                                    </div>
                                    ${(room.waterBill?.amount || 0) > 0 ? `<br><small class="text-muted">${room.waterBill?.units || 0} คน</small>` : ''}
                                    ${room.waterBill?.isPaid ? '<br><i class="bi bi-check-circle text-success"></i>' : ''}
                                </td>
                                <td class="text-center">
                                    <div class="d-flex align-items-center justify-content-center gap-1">
                                        <span class="bill-amount ${room.electricBill?.isPaid ? 'amount-paid' : 'amount-unpaid'}">
                                            ฿${(room.electricBill?.amount || 0).toLocaleString()}
                                        </span>
                                        ${(room.electricBill?.amount || 0) > 0 && !room.electricBill?.isPaid ? 
                                            `<button class="btn btn-sm btn-outline-warning" onclick="editElectricBill(${room.roomId}, '${room.roomName}')" title="แก้ไขค่าไฟ">
                                                <i class="bi bi-pencil"></i>
                                            </button>` : ''
                                        }
                                        ${(room.electricBill?.amount || 0) === 0 ? 
                                            `<button class="btn btn-sm btn-warning" onclick="addElectricBill(${room.roomId}, '${room.roomName}')" title="เพิ่มค่าไฟ">
                                                <i class="bi bi-plus"></i>
                                            </button>` : ''
                                        }
                                    </div>
                                    ${(room.electricBill?.amount || 0) > 0 ? `<br><small class="text-muted">${room.electricBill?.units || 0} หน่วย</small>` : ''}
                                    ${room.electricBill?.isPaid ? '<br><i class="bi bi-check-circle text-success"></i>' : ''}
                                </td>
                                <td class="text-center">
                                    <span class="bill-amount fw-bold text-primary fs-5">
                                        ฿${(room.totalAmount || 0).toLocaleString()}
                                    </span>
                                    ${(room.pendingAmount || 0) > 0 ? `<br><small class="text-danger">ค้าง ฿${(room.pendingAmount || 0).toLocaleString()}</small>` : ''}
                                </td>
                                <td class="text-center">
                                    <div class="progress progress-mini mb-1" style="height: 8px;">
                                        <div class="progress-bar ${progressClass}" role="progressbar" 
                                             style="width: ${progressWidth}%" aria-valuenow="${progressWidth}" 
                                             aria-valuemin="0" aria-valuemax="100"></div>
                                    </div>
                                    <small class="text-muted">${progressWidth}%</small>
                                </td>
                                <td class="text-center">
                                    <div class="btn-group btn-group-sm" role="group">
                                        <button class="btn btn-outline-info" onclick="showRoomDetail(${room.roomId})" title="รายละเอียด">
                                            <i class="bi bi-eye"></i>
                                        </button>
                                        ${!room.isFullyPaid ? `
                                            <button class="btn btn-outline-success" onclick="markRoomAsPaid(${room.roomId})" title="เก็บเงินครบ">
                                                <i class="bi bi-check-circle"></i>
                                            </button>
                                        ` : ''}
                                        <div class="btn-group" role="group">
                                            <button type="button" class="btn btn-outline-secondary dropdown-toggle dropdown-toggle-split" 
                                                    data-bs-toggle="dropdown" aria-expanded="false" title="เพิ่มเติม">
                                                <i class="bi bi-three-dots"></i>
                                            </button>
                                            <ul class="dropdown-menu">
                                                <li><a class="dropdown-item" href="#" onclick="showPaymentHistory(${room.roomId})">
                                                    <i class="bi bi-clock-history me-2"></i>ประวัติการชำระ
                                                </a></li>
                                                <li><a class="dropdown-item" href="#" onclick="printRoomReceipt(${room.roomId})">
                                                    <i class="bi bi-receipt me-2"></i>พิมพ์ใบเสร็จ
                                                </a></li>
                                                <li><a class="dropdown-item" href="#" onclick="manageTenant(${room.roomId}, '${room.roomName}')">
                                                    <i class="bi bi-person-fill me-2"></i>จัดการผู้เช่า
                                                </a></li>
                                            </ul>
                                        </div>
                                    </div>
                                </td>
                            </tr>
                        `;
                    } catch (roomError) {
                        console.error('Error rendering room:', room, roomError);
                        tableBody += `
                            <tr class="room-row" data-room-id="${room.roomId || 'unknown'}">
                                <td colspan="9" class="text-center text-danger">
                                    เกิดข้อผิดพลาดในการแสดงข้อมูลห้อง ${room.roomName || 'ไม่ทราบ'}
                                </td>
                            </tr>
                        `;
                    }
                });
            }

            $('#missionTableBody').html(tableBody);
            updateTableStats(filteredData.length, missionData.length);
            
            console.log('Render table เสร็จสิ้น');
        }

        // ฟังก์ชันช่วยเหลือ
        function getPriorityClass(room) {
            if (room.roomCharge.daysOverdue > 7) return 'priority-high';
            if (room.roomCharge.daysOverdue > 0 || room.isPartiallyPaid) return 'priority-medium';
            if (room.isFullyPaid) return 'priority-normal';
            return '';
        }

        function getProgressClass(progress) {
            if (progress >= 100) return 'bg-success';
            if (progress >= 50) return 'bg-warning';
            return 'bg-danger';
        }

        function applyCurrentFilter(data) {
            let filtered = data;

            // ฟิลเตอร์ตามประเภท
            switch (currentFilter) {
                case 'unpaid':
                    filtered = filtered.filter(room => !room.isFullyPaid && !room.isPartiallyPaid);
                    break;
                case 'partial':
                    filtered = filtered.filter(room => room.isPartiallyPaid);
                    break;
                case 'overdue':
                    filtered = filtered.filter(room => room.roomCharge.daysOverdue > 0);
                    break;
            }

            // ซ่อนห้องที่จ่ายครบ
            if ($('#hideCompletedRooms').is(':checked')) {
                filtered = filtered.filter(room => !room.isFullyPaid);
            }

            return filtered;
        }

        function applyFilter() {
            renderMissionTable();
        }

        function updateSelectedRooms() {
            selectedRooms.clear();
            $('.room-checkbox:checked').each(function () {
                selectedRooms.add(parseInt($(this).val()));
            });

            $('#bulkCollectBtn').prop('disabled', selectedRooms.size === 0);
            
            // อัปเดตสถานะ select all
            const totalCheckboxes = $('.room-checkbox').length;
            const checkedCheckboxes = $('.room-checkbox:checked').length;
            
            $('#selectAllRooms').prop('checked', checkedCheckboxes === totalCheckboxes && totalCheckboxes > 0);
            $('#selectAllRooms').prop('indeterminate', checkedCheckboxes > 0 && checkedCheckboxes < totalCheckboxes);
        }

        function updateTableStats(visible, total) {
            $('#visibleRooms').text(visible);
            $('#totalRooms').text(total);
        }

        function updateMissionStats() {
            const unpaidCount = missionData.filter(room => !room.isFullyPaid).length;
            $('#missionBadge').text(unpaidCount);
            
            if (unpaidCount > 0) {
                $('#missionBadge').addClass('notification-badge');
            } else {
                $('#missionBadge').removeClass('notification-badge');
            }
        }

        // ฟังก์ชันการจัดการข้อมูล
        function calculateWaterBill() {
            const oldMeter = parseInt($('#old_meter').val()) || 0;
            const newMeter = parseInt($('#new_meter').val()) || 0;
            const units = Math.max(0, newMeter - oldMeter);
            const bill = units * rates.waterRate;

            $('#water_units').val(units);
            $('#water_bill').val(bill);
        }

        function calculateElectricBill() {
            const oldMeter = parseInt($('#electric_old_meter').val()) || 0;
            const newMeter = parseInt($('#electric_new_meter').val()) || 0;
            const units = Math.max(0, newMeter - oldMeter);
            const electricRate = rates.electricRate || 5;
            const bill = units * electricRate;

            $('#electric_units').val(units);
            $('#electric_bill_amount').val(bill);
        }

        function handleWaterMeterSubmit(e) {
            e.preventDefault();
            const formData = new FormData(this);
            const waterId = $('#water_meter_id').val();
            const isEdit = waterId && waterId.trim() !== '';
            
            const url = isEdit ? `/Finance/UpdateWaterMeter/${waterId}` : '/Finance/CreateWaterMeter';
            
            $.ajax({
                url: url,
                type: 'POST',
                data: formData,
                processData: false,
                contentType: false,
                success: function (response) {
                    if (response.success) {
                        $('#waterMeterModal').modal('hide');
                        refreshAllData();
                        showSuccess(isEdit ? 'แก้ไขข้อมูลค่าน้ำเรียบร้อยแล้ว' : 'บันทึกข้อมูลค่าน้ำเรียบร้อยแล้ว');
                    } else {
                        showError(response.message || 'ไม่สามารถบันทึกข้อมูลได้');
                    }
                },
                error: function () {
                    showError('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                }
            });
        }

        function handleElectricMeterSubmit(e) {
            e.preventDefault();
            const formData = new FormData(this);
            const electricId = $('#electric_meter_id').val();
            const isEdit = electricId && electricId.trim() !== '';
            
            const url = isEdit ? `/Finance/UpdateElectricMeter/${electricId}` : '/Finance/CreateElectricMeter';
            
            $.ajax({
                url: url,
                type: 'POST',
                data: formData,
                processData: false,
                contentType: false,
                success: function (response) {
                    if (response.success) {
                        $('#electricMeterModal').modal('hide');
                        refreshAllData();
                        showSuccess(isEdit ? 'แก้ไขข้อมูลค่าไฟเรียบร้อยแล้ว' : 'บันทึกข้อมูลค่าไฟเรียบร้อยแล้ว');
                    } else {
                        showError(response.message || 'ไม่สามารถบันทึกข้อมูลได้');
                    }
                },
                error: function () {
                    showError('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                }
            });
        }

        function handleRoomChargeSubmit(e) {
            e.preventDefault();
            const formData = new FormData(this);
            const roomChargeId = $('#room_charge_id').val();
            const isEdit = roomChargeId && roomChargeId.trim() !== '';
            
            const url = isEdit ? `/Finance/UpdateRoomCharge/${roomChargeId}` : '/Finance/CreateRoomCharge';
            
            $.ajax({
                url: url,
                type: 'POST',
                data: formData,
                processData: false,
                contentType: false,
                success: function (response) {
                    if (response.success) {
                        $('#roomChargeModal').modal('hide');
                        refreshAllData();
                        showSuccess(isEdit ? 'แก้ไขข้อมูลค่าห้องเรียบร้อยแล้ว' : 'บันทึกข้อมูลค่าห้องเรียบร้อยแล้ว');
                    } else {
                        showError(response.message || 'ไม่สามารถบันทึกข้อมูลได้');
                    }
                },
                error: function () {
                    showError('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                }
            });
        }

        // ฟังก์ชันสำหรับการดำเนินการต่างๆ
        function refreshAllData() {
            updateStatistics();
            loadMissionData();
        }

        function generateMonthlyCharges() {
            Swal.fire({
                title: 'สร้างค่าห้องประจำเดือน',
                text: 'คุณต้องการสร้างค่าห้องประจำเดือนสำหรับทุกห้องใช่หรือไม่?',
                icon: 'question',
                showCancelButton: true,
                confirmButtonColor: '#28a745',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ใช่, สร้างเลย',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.post('/Finance/GenerateMonthlyCharges', function (response) {
                        if (response.success) {
                            refreshAllData();
                            Swal.fire({
                                title: 'สำเร็จ!',
                                html: `${response.message}<br>
                                      <strong>สร้างใหม่:</strong> ${response.createdCount} ห้อง<br>
                                      <strong>มีอยู่แล้ว:</strong> ${response.skippedCount} ห้อง`,
                                icon: 'success'
                            });
                        } else {
                            showError(response.message);
                        }
                    }).fail(() => showError('ไม่สามารถเชื่อมต่อเซิร์ฟเวอร์ได้'));
                }
            });
        }

        function markRoomAsPaid(roomId) {
            const room = missionData.find(r => r.roomId === roomId);
            if (!room) return;

            Swal.fire({
                title: 'ยืนยันการเก็บเงิน',
                html: `<div class="text-center">
                    <h5>ห้อง ${room.roomName}</h5>
                    <p class="mb-2">ยอดรวมที่ต้องเก็บ: <strong class="text-primary">฿${room.pendingAmount.toLocaleString()}</strong></p>
                    <div class="alert alert-info">
                        <small>การดำเนินการนี้จะทำเครื่องหมายว่าเก็บเงินครบทุกรายการแล้ว</small>
                    </div>
                </div>`,
                icon: 'question',
                showCancelButton: true,
                confirmButtonColor: '#28a745',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ยืนยันเก็บเงิน',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.post('/Finance/MarkRoomAsPaid', { roomId: roomId }, function (response) {
                        if (response.success) {
                            refreshAllData();
                            showSuccess(`เก็บเงินห้อง ${room.roomName} เรียบร้อยแล้ว`);
                        } else {
                            showError(response.message);
                        }
                    }).fail(() => showError('ไม่สามารถบันทึกการชำระเงินได้'));
                }
            });
        }

        function bulkCollectPayment() {
            if (selectedRooms.size === 0) return;

            const selectedRoomNames = Array.from(selectedRooms).map(id => {
                const room = missionData.find(r => r.roomId === id);
                return room ? room.roomName : '';
            }).filter(name => name).join(', ');

            Swal.fire({
                title: 'เก็บเงินหลายห้อง',
                html: `<div class="text-center">
                    <p>คุณต้องการเก็บเงินห้องต่อไปนี้ใช่หรือไม่?</p>
                    <div class="alert alert-info">
                        <strong>ห้องที่เลือก:</strong><br>${selectedRoomNames}
                    </div>
                    <p class="text-muted"><small>จำนวน ${selectedRooms.size} ห้อง</small></p>
                </div>`,
                icon: 'question',
                showCancelButton: true,
                confirmButtonColor: '#28a745',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ยืนยันเก็บเงิน',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: '/Finance/BulkMarkAsPaid',
                        type: 'POST',
                        data: JSON.stringify(Array.from(selectedRooms)),
                        contentType: 'application/json',
                        success: function (response) {
                            if (response.success) {
                                refreshAllData();
                                selectedRooms.clear();
                                updateSelectedRooms();
                                showSuccess(`เก็บเงินเรียบร้อยแล้ว ${response.successCount}/${response.totalCount} ห้อง`);
                            } else {
                                showError(response.message);
                            }
                        },
                        error: function () {
                            showError('ไม่สามารถเก็บเงินได้');
                        }
                    });
                }
            });
        }

        // ฟังก์ชัน UI และการแสดงผล
        function toggleViewMode() {
            const isMissionView = $('#missionView').is(':checked');
            $('#missionViewCard').toggle(isMissionView);
            $('#detailViewCard').toggle(!isMissionView);
            
            if (!isMissionView && !$.fn.DataTable.isDataTable('#waterMeterTable')) {
                initializeDetailTables();
            }
        }

        function showLoading(selector) {
            $(selector).html('<tr><td colspan="9" class="text-center py-4"><div class="spinner-border text-primary" role="status"><span class="visually-hidden">กำลังโหลด...</span></div></td></tr>');
        }

        function hideLoading(selector) {
            // Loading will be replaced by actual content
        }

        function showSuccess(message) {
            Swal.fire({
                title: 'สำเร็จ!',
                text: message,
                icon: 'success',
                toast: true,
                position: 'top-end',
                showConfirmButton: false,
                timer: 3000,
                timerProgressBar: true
            });
        }

        function showError(message) {
            Swal.fire({
                title: 'เกิดข้อผิดพลาด!',
                text: message,
                icon: 'error',
                toast: true,
                position: 'top-end',
                showConfirmButton: false,
                timer: 5000,
                timerProgressBar: true
            });
        }

        // ฟังก์ชันอื่นๆ
        function setupAutoRefresh() {
            // Auto refresh every 5 minutes if enabled
        }

        function startAutoRefresh() {
            if (autoRefreshInterval) clearInterval(autoRefreshInterval);
            autoRefreshInterval = setInterval(refreshAllData, 300000); // 5 minutes
        }

        function stopAutoRefresh() {
            if (autoRefreshInterval) {
                clearInterval(autoRefreshInterval);
                autoRefreshInterval = null;
            }
        }

        // ฟังก์ชันอื่นๆ ที่ยังไม่ได้ implement
        function createMission() {
            $('#createMissionBtn').addClass('active');
            setTimeout(() => {
                $('#createMissionBtn').removeClass('active');
            }, 500);
            
            // เน้นไปที่ Mission Table
            $('html, body').animate({
                scrollTop: $('#missionViewCard').offset().top - 100
            }, 800);
        }

        function printMission() {
            window.open('/Finance/PrintMissionList', '_blank');
        }

        function markAllAsPaid() {
            if (missionData.length === 0) {
                showError('ไม่มีข้อมูลห้อง');
                return;
            }

            const unpaidRooms = missionData.filter(room => !room.isFullyPaid);
            if (unpaidRooms.length === 0) {
                showError('ทุกห้องจ่ายครบแล้ว');
                return;
            }

            Swal.fire({
                title: 'เก็บเงินทุกห้อง',
                html: `คุณต้องการทำเครื่องหมายเก็บเงินครบทุกห้องใช่หรือไม่?<br>
                       <strong>จำนวนห้องที่ยังไม่จ่าย:</strong> ${unpaidRooms.length} ห้อง`,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#28a745',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ใช่, เก็บเงินทั้งหมด',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    const roomIds = unpaidRooms.map(room => room.roomId);
                    bulkCollectPaymentInternal(roomIds);
                }
            });
        }

        function showOverdueReport() {
            $.get('/Finance/GetOverdueReport', function(response) {
                if (response.success) {
                    let reportHtml = `
                        <div class="alert alert-info">
                            <h6><i class="bi bi-info-circle me-2"></i>สรุปรายงานค้างชำระ</h6>
                            <p class="mb-1">รายการค้างชำระ: <strong>${response.summary.totalItems}</strong> รายการ</p>
                            <p class="mb-1">ยอดรวม: <strong>฿${response.summary.totalAmount.toLocaleString()}</strong></p>
                            <p class="mb-0">เฉลี่ยค้างชำระ: <strong>${response.summary.averageDaysOverdue}</strong> วัน</p>
                        </div>
                    `;

                    if (response.overdueItems.length > 0) {
                        reportHtml += `
                            <div class="table-responsive">
                                <table class="table table-sm table-striped">
                                    <thead>
                                        <tr>
                                            <th>ห้อง</th>
                                            <th>ประเภท</th>
                                            <th>จำนวนเงิน</th>
                                            <th>วันครบกำหนด</th>
                                            <th>วันค้างชำระ</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                        `;

                        response.overdueItems.forEach(item => {
                            reportHtml += `
                                <tr>
                                    <td>${item.roomName}</td>
                                    <td>${item.type}</td>
                                    <td class="text-end">฿${item.amount.toLocaleString()}</td>
                                    <td>${moment(item.dueDate).format('DD/MM/YYYY')}</td>
                                    <td><span class="badge bg-danger">${item.daysOverdue} วัน</span></td>
                                </tr>
                            `;
                        });

                        reportHtml += '</tbody></table></div>';
                    } else {
                        reportHtml += '<div class="alert alert-success">ไม่มีรายการค้างชำระ</div>';
                    }

                    Swal.fire({
                        title: 'รายงานค้างชำระ',
                        html: reportHtml,
                        width: '800px',
                        showCloseButton: true,
                        focusConfirm: false,
                        confirmButtonText: 'ปิด'
                    });
                } else {
                    showError('ไม่สามารถสร้างรายงานได้');
                }
            }).fail(() => showError('เกิดข้อผิดพลาดในการดึงข้อมูลรายงาน'));
        }

        function exportMissionData() {
            window.location.href = '/Finance/ExportMissionData';
        }

        function bulkCollectPaymentInternal(roomIds) {
            $.ajax({
                url: '/Finance/BulkMarkAsPaid',
                type: 'POST',
                data: JSON.stringify(roomIds),
                contentType: 'application/json',
                success: function (response) {
                    if (response.success) {
                        refreshAllData();
                        showSuccess(`เก็บเงินเรียบร้อยแล้ว ${response.successCount}/${response.totalCount} ห้อง`);
                    } else {
                        showError(response.message);
                    }
                },
                error: function () {
                    showError('ไม่สามารถเก็บเงินได้');
                }
            });
        }

        // Global functions สำหรับเรียกใช้จาก HTML และ DataTables
        window.showRoomDetail = function(roomId) {
            $.get('/Finance/GetRoomDetail', { roomId: roomId }, function(response) {
                if (response.success) {
                    const room = response.data.roomInfo;
                    const history = response.data.paymentHistory;
                    
                    let detailHtml = `
                        <div class="row mb-3">
                            <div class="col-md-6">
                                <h6><i class="bi bi-house-door me-2"></i>ข้อมูลห้อง</h6>
                                <p class="mb-1"><strong>ห้อง:</strong> ${room.roomName}</p>
                                <p class="mb-1"><strong>ผู้เช่า:</strong> ${room.tenantName}</p>
                                <p class="mb-0"><strong>ยอดรวมเดือนนี้:</strong> ฿${room.totalAmount.toLocaleString()}</p>
                            </div>
                            <div class="col-md-6">
                                <h6><i class="bi bi-credit-card me-2"></i>สถานะการชำระ</h6>
                                <div class="progress mb-2">
                                    <div class="progress-bar ${room.paymentProgress >= 100 ? 'bg-success' : room.paymentProgress >= 50 ? 'bg-warning' : 'bg-danger'}" 
                                         style="width: ${room.paymentProgress}%">${room.paymentProgress}%</div>
                                </div>
                                <p class="mb-0">
                                    <span class="badge ${room.isFullyPaid ? 'bg-success' : room.isPartiallyPaid ? 'bg-warning' : 'bg-danger'}">
                                        ${room.isFullyPaid ? 'จ่ายครบ' : room.isPartiallyPaid ? 'จ่ายบางส่วน' : 'ยังไม่จ่าย'}
                                    </span>
                                </p>
                            </div>
                        </div>
                        
                        <div class="row mb-3">
                            <div class="col-md-4 text-center">
                                <div class="card border-primary">
                                    <div class="card-body">
                                        <h6 class="card-title">ค่าห้อง</h6>
                                        <h5 class="text-primary">฿${room.roomCharge.amount.toLocaleString()}</h5>
                                        <span class="badge ${room.roomCharge.isPaid ? 'bg-success' : 'bg-danger'}">
                                            ${room.roomCharge.isPaid ? 'จ่ายแล้ว' : 'ยังไม่จ่าย'}
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-4 text-center">
                                <div class="card border-info">
                                    <div class="card-body">
                                        <h6 class="card-title">ค่าน้ำ</h6>
                                        <h5 class="text-info">฿${room.waterBill.amount.toLocaleString()}</h5>
                                        <small class="text-muted">${room.waterBill.units} หน่วย</small><br>
                                        <span class="badge ${room.waterBill.isPaid ? 'bg-success' : 'bg-danger'}">
                                            ${room.waterBill.isPaid ? 'จ่ายแล้ว' : 'ยังไม่จ่าย'}
                                        </span>
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-4 text-center">
                                <div class="card border-warning">
                                    <div class="card-body">
                                        <h6 class="card-title">ค่าไฟ</h6>
                                        <h5 class="text-warning">฿${room.electricBill.amount.toLocaleString()}</h5>
                                        <small class="text-muted">${room.electricBill.units} หน่วย</small><br>
                                        <span class="badge ${room.electricBill.isPaid ? 'bg-success' : 'bg-danger'}">
                                            ${room.electricBill.isPaid ? 'จ่ายแล้ว' : 'ยังไม่จ่าย'}
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    `;

                    $('#roomDetailContent').html(detailHtml);
                    $('#roomDetailModalLabel').text(`รายละเอียดห้อง ${room.roomName}`);
                    $('#roomDetailModal').modal('show');
                } else {
                    showError('ไม่สามารถดึงข้อมูลรายละเอียดได้');
                }
            }).fail(() => showError('เกิดข้อผิดพลาดในการดึงข้อมูล'));
        };

        window.showPaymentHistory = function(roomId) {
            $.get('/Finance/GetPaymentHistory', { roomId: roomId, months: 6 }, function(response) {
                if (response.success) {
                    let historyHtml = '<div class="row">';
                    
                    // แสดงประวัติค่าห้อง
                    historyHtml += '<div class="col-md-4"><h6>ค่าห้อง</h6><div class="list-group">';
                    response.roomCharges.forEach(charge => {
                        historyHtml += `
                            <div class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <strong>${charge.month}</strong><br>
                                    <small>฿${charge.amount.toLocaleString()}</small>
                                </div>
                                <span class="badge ${charge.isPaid ? 'bg-success' : 'bg-danger'}">
                                    ${charge.isPaid ? 'จ่ายแล้ว' : 'ยังไม่จ่าย'}
                                </span>
                            </div>
                        `;
                    });
                    historyHtml += '</div></div>';

                    // แสดงประวัติค่าน้ำ
                    historyHtml += '<div class="col-md-4"><h6>ค่าน้ำ</h6><div class="list-group">';
                    response.waterBills.forEach(bill => {
                        historyHtml += `
                            <div class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <strong>${bill.month}</strong><br>
                                    <small>฿${bill.amount.toLocaleString()} (${bill.units} หน่วย)</small>
                                </div>
                                <span class="badge ${bill.isPaid ? 'bg-success' : 'bg-danger'}">
                                    ${bill.isPaid ? 'จ่ายแล้ว' : 'ยังไม่จ่าย'}
                                </span>
                            </div>
                        `;
                    });
                    historyHtml += '</div></div>';

                    // แสดงประวัติค่าไฟ
                    historyHtml += '<div class="col-md-4"><h6>ค่าไฟ</h6><div class="list-group">';
                    response.electricBills.forEach(bill => {
                        historyHtml += `
                            <div class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <strong>${bill.month}</strong><br>
                                    <small>฿${bill.amount.toLocaleString()} (${bill.units} หน่วย)</small>
                                </div>
                                <span class="badge ${bill.isPaid ? 'bg-success' : 'bg-danger'}">
                                    ${bill.isPaid ? 'จ่ายแล้ว' : 'ยังไม่จ่าย'}
                                </span>
                            </div>
                        `;
                    });
                    historyHtml += '</div></div></div>';

                    Swal.fire({
                        title: 'ประวัติการชำระเงิน',
                        html: historyHtml,
                        width: '900px',
                        showCloseButton: true,
                        focusConfirm: false,
                        confirmButtonText: 'ปิด'
                    });
                } else {
                    showError('ไม่สามารถดึงประวัติได้');
                }
            }).fail(() => showError('เกิดข้อผิดพลาดในการดึงประวัติ'));
        };

        window.printRoomReceipt = function(roomId) {
            // เรียกใช้ API เพื่อสร้างใบเสร็จ
            window.open('/Finance/GenerateRoomReceipt?roomId=' + roomId, '_blank');
        };

        // ฟังก์ชันสำหรับจัดการค่าน้ำและค่าไฟ
        window.addWaterBill = function(roomId, roomName) {
            $('#water_bill_id').val('');
            $('#water_room_id').val(roomId);
            $('#water_room_name').val(roomName);
            $('#water_people_count').val(1);
            // เพิ่มการตั้งค่า water_units ให้เท่ากับ people_count
            calculateWaterBillByPeople();
            $('#water_bill_notes').val('');
            $('#waterBillModalLabel').text('เพิ่มค่าน้ำ - ' + roomName);
            $('#waterBillModal').modal('show');
        };

        window.editWaterBill = function(roomId, roomName) {
            const room = missionData.find(r => r.roomId === roomId);
            if (room && room.waterBill.amount > 0) {
                $('#water_bill_id').val(room.waterBill.id || '');
                $('#water_room_id').val(roomId);
                $('#water_room_name').val(roomName);
                $('#water_people_count').val(room.waterBill.units || 1);
                $('#water_bill_amount').val(room.waterBill.amount);
                $('#water_bill_notes').val('');
                $('#waterBillModalLabel').text('แก้ไขค่าน้ำ - ' + roomName);
                $('#waterBillModal').modal('show');
            }
        };

        window.addElectricBill = function(roomId, roomName) {
            $('#electric_bill_id').val('');
            $('#electric_room_id').val(roomId);
            $('#electric_room_name').val(roomName);
            $('#electric_old_meter').val('');
            $('#electric_new_meter').val('');
            $('#electric_units').val(0);
            $('#electric_bill_amount').val(0);
            $('#electric_bill_notes').val('');
            // เพิ่มฟิลด์วันที่
            const now = moment().format('YYYY-MM-DD');
            $('#meter_date').val(now);
            $('#electricBillModalLabel').text('เพิ่มค่าไฟ - ' + roomName);
            $('#electricBillModal').modal('show');
        };

        window.editElectricBill = function(roomId, roomName) {
            const room = missionData.find(r => r.roomId === roomId);
            if (room && room.electricBill.amount > 0) {
                $('#electric_bill_id').val(room.electricBill.id || '');
                $('#electric_room_id').val(roomId);
                $('#electric_room_name').val(roomName);
                $('#electric_old_meter').val(0);
                $('#electric_new_meter').val(room.electricBill.units || 0);
                $('#electric_units').val(room.electricBill.units || 0);
                $('#electric_bill_amount').val(room.electricBill.amount);
                $('#electric_bill_notes').val('');
                $('#electricBillModalLabel').text('แก้ไขค่าไฟ - ' + roomName);
                $('#electricBillModal').modal('show');
            }
        };

        // คำนวณค่าน้ำตามจำนวนคน
        function calculateWaterBillByPeople() {
            const peopleCount = parseInt($('#water_people_count').val()) || 0;
            const waterRate = rates.waterRate || 150; // ใช้อัตราจาก settings หรือ 150 บาท/คน
            const totalBill = peopleCount * waterRate;
            $('#water_bill_amount').val(totalBill);
            $('#water_units').val(peopleCount); // ตั้งค่า water_units ให้เท่ากับจำนวนคน
        }

        // Event handlers สำหรับฟอร์มใหม่
        $('#water_people_count').on('input', calculateWaterBillByPeople);
        $('#electric_old_meter, #electric_new_meter').on('input', calculateElectricBill);

        // ฟอร์มสำหรับค่าน้ำ
        $('#waterBillForm').on('submit', function(e) {
            e.preventDefault();
            const formData = new FormData(this);
            const billId = $('#water_bill_id').val();
            const isEdit = billId && billId.trim() !== '';
            
            const url = isEdit ? `/Finance/UpdateWaterBill/${billId}` : '/Finance/CreateWaterBill';
            
            $.ajax({
                url: url,
                type: 'POST',
                data: formData,
                processData: false,
                contentType: false, 
                success: function (response) {
                    if (response.success) {
                        $('#waterBillModal').modal('hide');
                        refreshAllData();
                        showSuccess(isEdit ? 'แก้ไขค่าน้ำเรียบร้อยแล้ว' : 'เพิ่มค่าน้ำเรียบร้อยแล้ว');
                    } else {
                        showError(response.message || 'ไม่สามารถบันทึกข้อมูลได้');
                    }
                },
                error: function () {
                    showError('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                }
            });
        });

        // ฟอร์มสำหรับค่าไฟ
        $('#electricBillForm').on('submit', function(e) {
            e.preventDefault();
            const formData = new FormData(this);
            const billId = $('#electric_bill_id').val();
            const isEdit = billId && billId.trim() !== '';
            
            const url = isEdit ? `/Finance/UpdateElectricMeter/${billId}` : '/Finance/CreateElectricMeter';
            
            $.ajax({
                url: url,
                type: 'POST',
                data: formData,
                processData: false,
                contentType: false,
                success: function (response) {
                    if (response.success) {
                        $('#electricBillModal').modal('hide');
                        refreshAllData();
                        showSuccess(isEdit ? 'แก้ไขค่าไฟเรียบร้อยแล้ว' : 'เพิ่มค่าไฟเรียบร้อยแล้ว');
                    } else {
                        showError(response.message || 'ไม่สามารถบันทึกข้อมูลได้');
                    }
                },
                error: function () {
                    showError('เกิดข้อผิดพลาดในการบันทึกข้อมูล');
                }
            });
        });

        // ฟังก์ชันสำหรับการแก้ไขและลบข้อมูลใน Detail Tables
        window.editWaterMeter = function(id) {
            $.get('/Finance/GetWaterMeters', function(response) {
                const waterMeter = response.data.find(w => w.water_meter_id === id);
                if (waterMeter) {
                    $('#water_meter_id').val(waterMeter.water_meter_id);
                    $('#room_id').val(waterMeter.room_id);
                    $('#meter_date').val(waterMeter.meter_date);
                    $('#old_meter').val(waterMeter.old_meter);
                    $('#new_meter').val(waterMeter.new_meter);
                    $('#water_units').val(waterMeter.water_units);
                    $('#water_bill').val(waterMeter.water_bill);
                    $('#water_notes').val(waterMeter.notes);
                    
                    $('#waterMeterModalLabel').text('แก้ไขข้อมูลค่าน้ำ');
                    $('#waterMeterModal').modal('show');
                }
            });
        };

        window.editElectricMeter = function(id) {
            $.get('/Finance/GetElectricMeters', function(response) {
                const electricMeter = response.data.find(e => e.electric_meter_id === id);
                if (electricMeter) {
                    $('#electric_meter_id').val(electricMeter.electric_meter_id);
                    $('#electric_room_id').val(electricMeter.room_id);
                    $('#electric_meter_date').val(electricMeter.meter_date);
                    $('#electric_old_meter').val(electricMeter.old_meter);
                    $('#electric_new_meter').val(electricMeter.new_meter);
                    $('#electric_units').val(electricMeter.electric_units);
                    $('#electric_bill').val(electricMeter.electric_bill);
                    $('#electric_notes').val(electricMeter.notes);
                    
                    $('#electricMeterModalLabel').text('แก้ไขข้อมูลค่าไฟ');
                    $('#electricMeterModal').modal('show');
                }
            });
        };

        window.editRoomCharge = function(id) {
            $.get('/Finance/GetRoomCharges', function(response) {
                const roomCharge = response.data.find(r => r.room_charge_id === id);
                if (roomCharge) {
                    $('#room_charge_id').val(roomCharge.room_charge_id);
                    $('#room_charge_room_id').val(roomCharge.room_id);
                    $('#charge_month').val(roomCharge.charge_month);
                    $('#room_price').val(roomCharge.room_price);
                    $('#due_date').val(roomCharge.due_date);
                    $('#room_charge_notes').val(roomCharge.notes);
                    
                    $('#roomChargeModalLabel').text('แก้ไขข้อมูลค่าห้อง');
                    $('#roomChargeModal').modal('show');
                }
            });
        };

        window.deleteWaterMeter = function(id) {
            Swal.fire({
                title: 'ยืนยันการลบ',
                text: 'คุณต้องการลบข้อมูลค่าน้ำนี้ใช่หรือไม่?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#dc3545',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ใช่, ลบเลย',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: `/Finance/DeleteWaterMeter/${id}`,
                        type: 'DELETE',
                        success: function(response) {
                            if (response.success) {
                                refreshDetailTables();
                                showSuccess('ลบข้อมูลเรียบร้อยแล้ว');
                            } else {
                                showError(response.message);
                            }
                        },
                        error: function() {
                            showError('ไม่สามารถลบข้อมูลได้');
                        }
                    });
                }
            });
        };

        window.deleteElectricMeter = function(id) {
            Swal.fire({
                title: 'ยืนยันการลบ',
                text: 'คุณต้องการลบข้อมูลค่าไฟนี้ใช่หรือไม่?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#dc3545',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ใช่, ลบเลย',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: `/Finance/DeleteElectricMeter/${id}`,
                        type: 'DELETE',
                        success: function(response) {
                            if (response.success) {
                                refreshDetailTables();
                                showSuccess('ลบข้อมูลเรียบร้อยแล้ว');
                            } else {
                                showError(response.message);
                            }
                        },
                        error: function() {
                            showError('ไม่สามารถลบข้อมูลได้');
                        }
                    });
                }
            });
        };

        window.deleteRoomCharge = function(id) {
            Swal.fire({
                title: 'ยืนยันการลบ',
                text: 'คุณต้องการลบข้อมูลค่าห้องนี้ใช่หรือไม่?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#dc3545',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ใช่, ลบเลย',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: `/Finance/DeleteRoomCharge/${id}`,
                        type: 'DELETE',
                        success: function(response) {
                            if (response.success) {
                                refreshDetailTables();
                                showSuccess('ลบข้อมูลเรียบร้อยแล้ว');
                            } else {
                                showError(response.message);
                            }
                        },
                        error: function() {
                            showError('ไม่สามารถลบข้อมูลได้');
                        }
                    });
                }
            });
        };

        // ล้างฟอร์มเมื่อปิด modal
        $('.modal').on('hidden.bs.modal', function () {
            const form = $(this).find('form')[0];
            if (form) {
                form.reset();
            }
            $(this).find('input[type="hidden"]').val('');
            
            // รีเซ็ต modal title เป็นค่าเริ่มต้น
            if ($(this).attr('id') === 'waterMeterModal') {
                $('#waterMeterModalLabel').text('บันทึกค่าน้ำ');
            } else if ($(this).attr('id') === 'electricMeterModal') {
                $('#electricMeterModalLabel').text('บันทึกค่าไฟ');
            } else if ($(this).attr('id') === 'roomChargeModal') {
                $('#roomChargeModalLabel').text('บันทึกค่าห้อง');
            }
        });

        // ป้องกันการ submit ฟอร์มซ้ำ
        $('form').on('submit', function() {
            const submitBtn = $(this).find('button[type="submit"]');
            submitBtn.prop('disabled', true);
            setTimeout(() => {
                submitBtn.prop('disabled', false);
            }, 3000);
        });

        function setupEventListeners() {
            // การเลือกห้องทั้งหมด
            $('#selectAllRooms').on('change', function () {
                const isChecked = $(this).prop('checked');
                $('.room-checkbox').prop('checked', isChecked);
                updateSelectedRooms();
            });

            // การเลือกห้องแต่ละห้อง
            $(document).on('change', '.room-checkbox', function () {
                updateSelectedRooms();
            });

            // ฟิลเตอร์ข้อมูล
            $('.filter-option').on('click', function (e) {
                e.preventDefault();
                currentFilter = $(this).data('filter');
                applyFilter();
            });

            // ปุ่มต่างๆ
            $('#refreshDataBtn').on('click', refreshAllData);
            $('#createMissionBtn').on('click', createMission);
            $('#generateMonthlyChargesBtn').on('click', generateMonthlyCharges);
            $('#markAllPaidBtn').on('click', markAllAsPaid);
            $('#printMissionBtn').on('click', printMission);
            $('#overdueReportBtn').on('click', showOverdueReport);
            $('#bulkCollectBtn').on('click', bulkCollectPayment);
            $('#exportMissionBtn').on('click', exportMissionData);

            // ฟอร์มการบันทึกข้อมูล
            $('#roomChargeForm').on('submit', handleRoomChargeSubmit);

            // คำนวณอัตโนมัติ
            $('#electric_old_meter, #electric_new_meter').on('input', calculateElectricBill);

            // การตั้งค่าอัตโนมัติ
            $('#autoRefresh').on('change', function () {
                if ($(this).is(':checked')) {
                    startAutoRefresh();
                } else {
                    stopAutoRefresh();
                }
            });

            // ซ่อน/แสดงห้องที่จ่ายครบ
            $('#hideCompletedRooms').on('change', function () {
                applyFilter();
            });
        }

        // ฟังก์ชันโหลดข้อมูล
        function loadRoomOptions() {
            // ลองใช้ข้อมูลจาก ViewBag ก่อน
            const viewBagRooms = @Html.Raw(Json.Serialize(ViewBag.Rooms ?? new object[0]));
            if (viewBagRooms && viewBagRooms.length > 0) {
                const options = viewBagRooms.map(room => 
                    `<option value="${room.room_id}">${room.room_name} (฿${room.room_price.toLocaleString()})</option>`
                ).join('');
                
                $('#room_charge_room_id').html(
                    '<option value="">-- เลือกห้อง --</option>' + options
                );
            } else {
                // ถ้าไม่มีข้อมูลจาก ViewBag ให้ดึงจาก API
                $.get('/Rooms/GetAvailableRooms', function (response) {
                    let rooms = [];
                    if (response.success && response.data) {
                        rooms = response.data;
                    } else if (response.length) {
                        rooms = response; // ในกรณีที่ API ส่งข้อมูลโดยตรง
                    }
                    
                    if (rooms.length > 0) {
                        const options = rooms.map(room => 
                            `<option value="${room.room_id}">${room.room_name} (฿${room.room_price.toLocaleString()})</option>`
                        ).join('');
                        
                        $('#room_charge_room_id').html(
                            '<option value="">-- เลือกห้อง --</option>' + options
                        );
                    }
                }).fail(function() {
                    console.error('ไม่สามารถโหลดรายการห้องได้');
                });
            }
        }

        function loadRates() {
            $.get('/Finance/GetRates', function (data) {
                rates = data;
                $('#waterRateDisplay').text(data.waterRate || 150);
                $('#electricRateDisplay').text(data.electricRate);
            });
        }

        function updateStatistics() {
            console.log('เริ่มโหลดสถิติ...');
            
            $.ajax({
                url: '/Finance/GetStatistics',
                type: 'GET',
                timeout: 15000, // 15 วินาที
                success: function (data) {
                    console.log('ได้รับข้อมูลสถิติ:', data);
                    
                    // อัปเดตสถิติพื้นฐาน
                    $('#totalIncome').text('฿' + (data.totalIncome || 0).toLocaleString());
                    $('#totalCollected').text('฿' + (data.totalCollected || 0).toLocaleString());
                    $('#totalPending').text('฿' + (data.totalPending || 0).toLocaleString());
                    $('#pendingRoomsCount').text(data.pendingRooms || 0);

                    // คำนวณเปอร์เซ็นต์
                    const collectionRate = data.analytics?.collectionRate || 0;
                    const totalIncome = data.totalIncome || 0;
                    const totalCollected = data.totalCollected || 0;
                    const collectedPct = totalIncome > 0 ? Math.round((totalCollected / totalIncome) * 100) : 0;
                    const pendingPct = 100 - collectedPct;

                    $('#collectionRate').text(collectionRate + '%');
                    $('#collectedPercentage').text(collectedPct + '% ของยอดรวม');
                    $('#pendingPercentage').text(pendingPct + '% ของยอดรวม');
                    
                    // อัปเดตแถบความคืบหน้า
                    $('#collectionProgress').css('width', collectionRate + '%').attr('aria-valuenow', collectionRate);

                    // แสดงการเติบโต
                    if (data.analytics?.growthRate !== undefined) {
                        const growthRate = data.analytics.growthRate;
                        const growthClass = growthRate >= 0 ? 'text-success' : 'text-danger';
                        const growthIcon = growthRate >= 0 ? '📈' : '📉';
                        $('#incomeGrowth').html(`${growthIcon} ${growthRate}%`).removeClass('text-success text-danger').addClass(growthClass);
                    }

                    // เฉลี่ยต่อห้อง
                    if (data.analytics?.averagePerRoom) {
                        $('#averageAmount').text(`เฉลี่ย ฿${data.analytics.averagePerRoom.toLocaleString()}/ห้อง`);
                    }
                    
                    console.log('อัปเดตสถิติเสร็จสิ้น');
                },
                error: function (xhr, status, error) {
                    console.error('เกิดข้อผิดพลาดในการโหลดสถิติ:', { xhr, status, error });
                    
                    // แสดงค่าเริ่มต้นเมื่อเกิดข้อผิดพลาด
                    $('#totalIncome').text('฿0');
                    $('#totalCollected').text('฿0');
                    $('#totalPending').text('฿0');
                    $('#pendingRoomsCount').text('0');
                    $('#collectionRate').text('0%');
                    $('#collectedPercentage').text('0% ของยอดรวม');
                    $('#pendingPercentage').text('0% ของยอดรวม');
                    $('#collectionProgress').css('width', '0%').attr('aria-valuenow', 0);
                    $('#averageAmount').text('เฉลี่ย ฿0/ห้อง');
                    
                    if (status !== 'abort') { // ไม่แสดง error ถ้าเป็นการยกเลิก
                        console.warn('ไม่สามารถโหลดสถิติได้ แสดงค่าเริ่มต้น');
                    }
                }
            });
        }

        // ฟังก์ชันสำหรับจัดการผู้เช่า
        window.manageTenant = function(roomId, roomName) {
            $('#tenant_room_id').val(roomId);
            $('#tenantModalLabel').text('จัดการผู้เช่า - ห้อง ' + roomName);
            
            // เคลียร์ค่า
            $('#tenant_select').val('');
            $('#tenant_start_date').val('');
            $('#tenant_end_date').val('');
            
            // โหลดข้อมูลผู้เช่าปัจจุบัน
            loadCurrentTenant(roomId);
            
            // โหลดรายชื่อผู้ใช้
            loadAvailableUsers();
            
            // แสดง modal
            $('#tenantModal').modal('show');
        };

        function loadCurrentTenant(roomId) {
            $.get('/Rooms/GetRoomTenant', { roomId: roomId }, function(response) {
                if (response.success) {
                    if (response.hasTenant) {
                        // มีผู้เช่า
                        $('#current_tenant_name').text(response.tenant.fullname || '-');
                        $('#current_tenant_email').text(response.tenant.email || '-');
                        $('#current_tenant_phone').text(response.tenant.phone || '-');
                        $('#current_tenant_start_date').text(response.tenant.start_date || '-');
                        $('#current_tenant_end_date').text(response.tenant.end_date || '-');
                        
                        $('#no_tenant_alert').hide();
                        $('#tenant_details').show();
                    } else {
                        // ไม่มีผู้เช่า
                        $('#no_tenant_alert').show();
                        $('#tenant_details').hide();
                    }
                } else {
                    showError(response.message || 'ไม่สามารถโหลดข้อมูลผู้เช่าได้');
                }
            }).fail(function() {
                showError('เกิดข้อผิดพลาดในการโหลดข้อมูลผู้เช่า');
            });
        }

        function loadAvailableUsers() {
            $.get('/Rooms/GetAvailableUsers', function(response) {
                if (response.success && response.users) {
                    const selectElement = $('#tenant_select');
                    selectElement.html('<option value="">-- เลือกผู้เช่า --</option>');
                    
                    response.users.forEach(function(user) {
                        selectElement.append(`<option value="${user.id}">${user.fullname || user.email || 'ผู้ใช้ ' + user.id}</option>`);
                    });
                } else {
                    showError(response.message || 'ไม่สามารถโหลดข้อมูลผู้ใช้ได้');
                }
            }).fail(function() {
                showError('เกิดข้อผิดพลาดในการโหลดข้อมูลผู้ใช้');
            });
        }

        $('#save_tenant_btn').on('click', function() {
            const roomId = parseInt($('#tenant_room_id').val());
            const userId = parseInt($('#tenant_select').val());
            const startDate = $('#tenant_start_date').val();
            const endDate = $('#tenant_end_date').val();
            
            if (!roomId) {
                showError('ไม่พบข้อมูลห้อง');
                return;
            }
            
            if (!userId) {
                showError('กรุณาเลือกผู้เช่า');
                return;
            }
            
            $.ajax({
                url: '/Rooms/UpdateRoomTenant',
                type: 'POST',
                data: {
                    roomId: roomId,
                    userId: userId,
                    startDate: startDate || null,
                    endDate: endDate || null
                },
                success: function(response) {
                    if (response.success) {
                        $('#tenantModal').modal('hide');
                        showSuccess(response.message || 'อัปเดตผู้เช่าเรียบร้อยแล้ว');
                        
                        // รีโหลดข้อมูล Mission ทั้งหมด
                        refreshAllData();
                    } else {
                        showError(response.message || 'ไม่สามารถอัปเดตผู้เช่าได้');
                    }
                },
                error: function() {
                    showError('เกิดข้อผิดพลาดในการอัปเดตผู้เช่า');
                }
            });
        });

        $('#remove_tenant_btn').on('click', function() {
            const roomId = parseInt($('#tenant_room_id').val());
            
            if (!roomId) {
                showError('ไม่พบข้อมูลห้อง');
                return;
            }
            
            Swal.fire({
                title: 'ยืนยันการยกเลิกผู้เช่า',
                text: 'คุณต้องการยกเลิกผู้เช่าห้องนี้ใช่หรือไม่?',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#dc3545',
                cancelButtonColor: '#6c757d',
                confirmButtonText: 'ใช่, ยกเลิกผู้เช่า',
                cancelButtonText: 'ยกเลิก'
            }).then((result) => {
                if (result.isConfirmed) {
                    $.ajax({
                        url: '/Rooms/RemoveRoomTenant',
                        type: 'POST',
                        data: { roomId: roomId },
                        success: function(response) {
                            if (response.success) {
                                $('#tenantModal').modal('hide');
                                showSuccess(response.message || 'ยกเลิกผู้เช่าเรียบร้อยแล้ว');
                                
                                // รีโหลดข้อมูล Mission ทั้งหมด
                                refreshAllData();
                            } else {
                                showError(response.message || 'ไม่สามารถยกเลิกผู้เช่าได้');
                            }
                        },
                        error: function() {
                            showError('เกิดข้อผิดพลาดในการยกเลิกผู้เช่า');
                        }
                    });
                }
            });
        });

        // สำหรับค่าเริ่มต้นที่รีเฟรชทุกครั้งที่เปิด modal
        $('#tenantModal').on('shown.bs.modal', function() {
            const today = moment().format('YYYY-MM-DD');
            const oneYearLater = moment().add(1, 'year').format('YYYY-MM-DD');
            
            if (!$('#tenant_start_date').val()) {
                $('#tenant_start_date').val(today);
            }
            
            if (!$('#tenant_end_date').val()) {
                $('#tenant_end_date').val(oneYearLater);
            }
        });
    </script>
}